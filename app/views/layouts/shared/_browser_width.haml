- if Rails.env.development?
  #browserwidth{style: "position:fixed; bottom:0; left:0; background:#555; color:#CCC; font-size:0.75em; padding:0.25em; text-shadow: 0 -1px 1px rgba(0,0,0,0.9); font-weight:normal;"}
  :javascript
  function showWidth(){var w = $(window).width();$('#browserwidth').text(w);}showWidth();$(window).resize(function(){showWidth();});