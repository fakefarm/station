- if signed_in?
  Signed in as: #{current_user.email}
  \#{link_to 'Sign out', sign_out_path, :method => :delete}
- else
  = link_to 'printer sign in', sign_in_path
#flash
  - flash.each do |key, value|
    %div{:class => "flash #{key}"}= value